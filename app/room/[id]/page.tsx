"use client";
import Link from "next/link";import {use,useEffect,useState} from "react";import {api} from "@/lib/api";import {Nav} from "@/components/Nav";import type {Item} from "@/lib/types";
export default function RoomPage({params}:{params:Promise<{id:string}>}){const {id}=use(params);const [items,setItems]=useState<any[]>([]);useEffect(()=>{api<any[]>(`/api/items?room_id=${id}`).then(setItems).catch(()=>{})},[id]);const room=items[0]?.rooms;return <main className="shell stack"><div className="topbar"><div><Link className="subtle" href="/">← All rooms</Link><h1 className="title">{room?.name||"Room"}</h1><div className="subtle">{items.length} items</div></div><Link className="btn primary" href={`/add?room=${id}`}>+ Add</Link></div>{items.length===0?<div className="card empty">Nothing photographed here yet.<br/><br/><Link className="btn primary" href={`/add?room=${id}`}>📸 Add first item</Link></div>:<section className="stack">{items.map(item=>{const wants=(item.votes||[]).filter((v:any)=>v.level==='want').length;return <Link className="card item-row" href={`/item/${item.id}`} key={item.id}>{item.photo_url?<img className="thumb" src={item.photo_url} alt=""/>:<div className="thumb"/>}<div><strong>{item.title}</strong><div className="badges" style={{marginTop:8}}><span className="badge">{item.destination}</span>{wants>1&&<span className="badge conflict">⚠️ {wants} want it</span>}</div></div></Link>})}</section>}<Nav/></main>}
